import React from 'react';

interface ExitIntentPopupProps {
  isTrialUser: boolean;
  userName: string;
}

export const ExitIntentPopup: React.FC<ExitIntentPopupProps> = ({ isTrialUser, userName }) => {
  return null; // Placeholder component
};

